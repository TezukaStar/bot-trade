# 🚀 Live Mode Setup Guide

คู่มือการตั้งค่า Trade Bot V1.4 สำหรับเชื่อมต่อกับ IQ Option บัญชีทดลอง (Practice Account)

---

## 📋 สิ่งที่ต้องเตรียม

1. ✅ **บัญชี IQ Option** (สมัครฟรีที่ [iqoption.com](https://iqoption.com))
2. ✅ **Python 3.11+** ติดตั้งในเครื่อง
3. ✅ **Dependencies** ติดตั้งครบ (ดูด้านล่าง)

---

## 🔧 ขั้นตอนการตั้งค่า

### **1. Clone Repository**

```bash
git clone https://github.com/your-username/bot-trade.git
cd bot-trade
```

### **2. ติดตั้ง Dependencies**

```bash
pip install -r requirements.txt
```

หรือติดตั้งแยกแต่ละตัว:

```bash
pip install iqoptionapi pandas numpy ta python-dotenv streamlit plotly rich
```

### **3. สร้างไฟล์ .env**

คัดลอก template:

```bash
cp .env.example .env
```

แก้ไขไฟล์ `.env` ใส่ credentials ของคุณ:

```bash
# .env
IQ_EMAIL=your_email@example.com
IQ_PASSWORD=your_password_here
IQ_MODE=PRACTICE
```

⚠️ **สำคัญ:**
- `.env` อยู่ใน `.gitignore` แล้ว (จะไม่ถูก commit)
- ใช้ `PRACTICE` mode สำหรับทดสอบ
- อย่า commit credentials เข้า git

### **4. ทดสอบการเชื่อมต่อ**

```bash
python bot_v1.4.py
```

ถ้าเชื่อมต่อสำเร็จจะเห็น:

```
✅ Bot initialized in PRACTICE mode
💰 Starting capital: $100
✅ Loaded config V1.4
🔌 Connecting to IQ Option (PRACTICE)...
✅ Connected successfully
✅ Switched to PRACTICE account
💰 Current balance: $10000.00
✅ Enabled pairs: EURUSD, EURUSD-OTC, EURCAD
⏰ Bot will run continuously until stopped (Ctrl+C)
🔄 Checking for signals every 30 seconds...
```

---

## 🤖 วิธีใช้งาน Bot

### **Practice Mode (แนะนำ)**

```bash
python bot_v1.4.py
```

หรือ

```bash
python bot_v1.4.py --mode=practice
```

### **Real Mode (ระวัง! เงินจริง)**

```bash
python bot_v1.4.py --mode=real
```

จะมีการถามยืนยัน:

```
⚠️  WARNING: Running in REAL mode. Are you sure? (yes/no):
```

---

## 📊 วิธีดู Dashboard ขณะ Bot ทำงาน

Bot จะบันทึกเทรดลง `trades.csv` อัตโนมัติ

### **ดู Dashboard แบบ Local:**

เปิด terminal ใหม่:

```bash
streamlit run dashboard.py
```

Dashboard จะเปิดที่: http://localhost:8501

### **ดู Dashboard บน Streamlit Cloud:**

1. Push โปรเจคขึ้น GitHub
2. Deploy บน [Streamlit Cloud](https://streamlit.io/cloud)
3. เปิด URL: `https://your-app.streamlit.app`

---

## ⚙️ การตั้งค่า Bot

### **แก้ไขกลยุทธ์:**

แก้ไขไฟล์: `versions/v1.4/config.json`

```json
{
  "capital": 100,
  "amount": 1,
  "payout": 0.8,

  "risk": {
    "stop_loss": 5,           // หยุดเมื่อขาดทุน $5
    "daily_loss_limit": 5,    // หยุดเมื่อขาดทุนต่อวัน $5
    "max_trades_per_day": 50  // เทรดสูงสุด 50 ครั้งต่อวัน
  },

  "currencies": {
    "EURUSD": {
      "enabled": true,        // เปิด/ปิดคู่เงิน
      "trading_hours": {
        "start": 19,          // เริ่มเทรดเวลา 19:00 UTC
        "end": 3              // หยุดเทรดเวลา 03:00 UTC
      },
      "session_filters": {
        "19-21": "put",       // 19:00-21:59 เทรด PUT เท่านั้น
        "22-2": "call"        // 22:00-02:59 เทรด CALL เท่านั้น
      },
      "indicators": {
        "adx_min": 8,         // ADX ต้อง >= 8
        "macd_min": 0.0005,   // MACD ต้อง >= 0.0005
        "price_ema_max": 0.005 // ระยะห่างจาก EMA20 สูงสุด 0.5%
      }
    }
  }
}
```

### **เปิด/ปิดคู่เงิน:**

```json
"EURUSD": {
  "enabled": true,  // true = เปิด, false = ปิด
  ...
}
```

### **ปรับ Risk Management:**

```json
"risk": {
  "stop_loss": 10,           // เพิ่มเป็น $10
  "daily_loss_limit": 10,
  "max_trades_per_day": 30   // ลดเหลือ 30 เทรด/วัน
}
```

---

## 🔍 การทำงานของ Bot

### **1. ตรวจสอบเงื่อนไข:**
- ✅ อยู่ในช่วงเวลาเทรดหรือไม่ (trading_hours)
- ✅ ตรง session filter หรือไม่ (session_filters)
- ✅ ADX, MACD, EMA ผ่านเกณฑ์หรือไม่
- ✅ Slope (เทรนด์) ชี้ทิศทางเดียวกับ signal หรือไม่

### **2. Execute Trade:**
- เมื่อมีสัญญาณ → ซื้อ Binary Option 1 นาที
- รอ 65 วินาที → เช็คผลลัพธ์
- บันทึกลง `trades.csv`

### **3. Risk Management:**
- เช็ค stop loss, daily loss limit, max trades
- ถ้าเกินขีด → หยุด bot อัตโนมัติ

---

## 📁 ไฟล์ที่ Bot สร้างขึ้น

### **trades.csv**

Bot จะบันทึกเทรดทุกครั้งลงไฟล์นี้:

```csv
trade_id,time,pair,direction,entry_price,amount,result,profit,capital,adx,macd,rsi,ema20
12345,2025-11-06 08:30:00,EURUSD-OTC,call,1.08234,1,win,0.8,100.8,15.2,0.0008,65.3,1.08150
```

Dashboard จะอ่านไฟล์นี้แล้วแสดงผล real-time

---

## 🛑 วิธีหยุด Bot

### **หยุดชั่วคราว:**

กด `Ctrl + C`

Bot จะบันทึกเทรดทั้งหมดแล้วปิดอย่างปลอดภัย

### **หยุดอัตโนมัติ:**

Bot จะหยุดเองเมื่อ:
- 🛑 ถึง stop loss ($5)
- 🛑 ถึง daily loss limit ($5)
- 🛑 เทรดครบ max trades per day (50 ครั้ง)

---

## 🚀 Deploy บน Cloud (Optional)

### **Option 1: VPS / Server**

```bash
# รัน bot แบบ background
nohup python bot_v1.4.py > bot.log 2>&1 &

# ดู log
tail -f bot.log

# หยุด bot
pkill -f bot_v1.4.py
```

### **Option 2: Docker**

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
CMD ["python", "bot_v1.4.py"]
```

```bash
docker build -t trade-bot .
docker run -d --name bot trade-bot
```

### **Option 3: ทิ้งไว้รันบนเครื่อง**

```bash
# Windows: สร้าง batch file
# run_bot.bat
@echo off
python bot_v1.4.py
pause

# Linux/Mac: สร้าง shell script
# run_bot.sh
#!/bin/bash
python3 bot_v1.4.py
```

---

## 📊 ดู Dashboard Real-time

### **เปิด Dashboard:**

```bash
streamlit run dashboard.py
```

เปิด browser: http://localhost:8501

### **Dashboard จะแสดง:**

- 💰 Total Profit, Win Rate, ROI
- 📈 Equity Curve (กราฟเงินทุน)
- 🥧 Win/Loss Pie Chart
- 📋 รายการเทรดทั้งหมด (มี pagination)
- 📊 แท็บแยกตามคู่เงิน

**Auto-refresh ทุก 10 วินาที** ดูผลได้แบบ real-time!

---

## ⚠️ ข้อควรระวัง

### **1. Practice Mode แนะนำสำหรับ:**
- ✅ ทดสอบกลยุทธ์
- ✅ เรียนรู้การใช้งาน
- ✅ Backtest ใหม่

### **2. Real Mode ใช้เมื่อ:**
- ✅ ทดสอบ Practice Mode แล้ว profitable
- ✅ เข้าใจ risk ทั้งหมด
- ✅ ตั้งค่า stop loss และ daily limit เหมาะสม
- ✅ เริ่มจากเงินจำนวนน้อย

### **3. Risk Management:**
- ⚠️ อย่าใช้เงินที่เสียแล้วเสียดาย
- ⚠️ ตั้ง stop loss และ daily limit ให้เหมาะสม
- ⚠️ เริ่มจาก capital น้อยๆ ($100-500)
- ⚠️ อย่า revenge trade (ขาดทุนแล้วเพิ่ม amount)

### **4. Network & Connection:**
- 📶 ต้องมี internet เสถียร
- 📶 ถ้าขาดการเชื่อมต่อ bot จะไม่ทำงาน
- 📶 แนะนำรันบน VPS หรือเครื่องที่เปิดตลอดเวลา

---

## 🐛 Troubleshooting

### **ปัญหา: เชื่อมต่อไม่ได้**

```
❌ Failed to connect: incorrect username or password
```

**วิธีแก้:**
- ตรวจสอบ email/password ใน `.env`
- ลอง login ที่ iqoption.com ดูว่าใช้งานได้ไหม
- ตรวจสอบ internet connection

### **ปัญหา: ไม่มีสัญญาณเทรด**

```
🔄 Checking for signals every 30 seconds...
(ไม่มีอะไรเกิดขึ้น)
```

**สาเหตุ:**
- ไม่อยู่ในช่วง trading_hours
- ไม่ตรง session_filters
- Indicators ไม่ผ่านเกณฑ์

**วิธีแก้:**
- รอให้ถึงช่วงเวลาที่กำหนด (ดู config.json)
- หรือลดความเข้มงวดของ indicators

### **ปัญหา: Dashboard ไม่แสดงข้อมูล**

```
⚠️ NO LIVE DATA
```

**วิธีแก้:**
- ตรวจสอบว่ามีไฟล์ `trades.csv` หรือไม่
- Bot ต้องเทรดอย่างน้อย 1 ครั้งก่อน
- Refresh dashboard (Ctrl + R)

### **ปัญหา: ModuleNotFoundError**

```
ModuleNotFoundError: No module named 'iqoptionapi'
```

**วิธีแก้:**
```bash
pip install iqoptionapi ta python-dotenv pandas numpy
```

---

## 📞 Support

หากมีปัญหา:

1. ✅ อ่านคู่มือนี้ให้ละเอียด
2. ✅ เช็ค logs ว่า error อะไร
3. ✅ ทดสอบใน Practice Mode ก่อน
4. ✅ เช็ค config.json ว่าตั้งค่าถูกต้องไหม

---

## 📈 Performance V1.4

**30-Day Backtest Results:**

| Metric | Value |
|--------|-------|
| Total Trades | 32 |
| Win Rate | 62.5% |
| Total Profit | $4.00 |
| ROI | +4.00% |
| Profit Factor | 1.67 |
| Max Drawdown | 4.70% |

**Enabled Pairs:**
- ✅ EURUSD: 100% win (1 trade)
- ✅ EURUSD-OTC: 59.09% win (22 trades)
- ✅ EURCAD: 66.67% win (9 trades)

---

## 🎯 Tips สำหรับผลลัพธ์ที่ดี

1. **ใจเย็น:** อย่าเพิ่ง revenge trade เมื่อขาดทุน
2. **Follow Strategy:** ใช้กลยุทธ์ที่ผ่าน backtest แล้ว
3. **Risk Management:** ตั้ง stop loss เสมอ
4. **Start Small:** เริ่มจากเงินน้อยๆ
5. **Monitor:** ดู dashboard เป็นระยะ
6. **Optimize:** ปรับ config ตามผลลัพธ์

---

**สร้างโดย:** Trade Bot V1.4
**อัพเดทล่าสุด:** November 6, 2025
**Status:** ✅ Production Ready

---

🚀 **Happy Trading!** 🚀
